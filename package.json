{
  "name": "sawata-research-hub",
  "version": "1.0.0",
  "description": "Sawata Research Hub - Institutional Repository",
  "scripts": {
    "build": "mkdir -p dist && cp -r index.html assets dist/",
    "dev": "npm run build && echo 'Static site ready in dist/'",
    "test": "echo 'No tests configured'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@supabase/supabase-js": "^2.90.1"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
